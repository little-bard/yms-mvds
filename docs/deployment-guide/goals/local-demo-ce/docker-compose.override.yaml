# Docker Compose Override 파일
# 개발자가 로컬에서 환경별 설정을 오버라이드하기 위한 파일
# 이 파일은 .gitignore에 추가하여 개인별 설정을 유지할 수 있습니다.

version: '3.8'

# 사용 예시:
# 1. 이 파일을 복사하여 필요한 서비스만 남기고 수정
# 2. 포트 변경, 환경 변수 추가, 볼륨 마운트 등 개인 설정
# 3. docker compose up 시 자동으로 적용됨

services:
  # 개발용 서비스 오버라이드 예시
  # consumer:
  #   ports:
  #     - "31000:80"  # 포트 변경
  #   environment:
  #     - DEBUG=true  # 환경 변수 추가

  # provider:
  #   volumes:
  #     - ./custom-config:/app/config  # 볼륨 추가

# 네트워크 오버라이드 예시
# networks:
#   edc-dev-network:
#     external: true

# 볼륨 오버라이드 예시  
# volumes:
#   custom-data:
#     external: true